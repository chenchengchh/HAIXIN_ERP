<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.hxcoe</groupId>
    <artifactId>system-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>System Parent</name>
    <description>Enterprise Management System Parent Project</description>

    <properties>
        <java.version>17</java.version>
        <spring.boot.version>3.2.0</spring.boot.version>
        <spring.cloud.version>2023.0.0</spring.cloud.version>
        <spring.cloud.alibaba.version>2022.0.0.0</spring.cloud.alibaba.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud Alibaba -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring.cloud.alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Spring Boot Maven Plugin -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Maven Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.30</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- Maven Surefire Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>
        </plugins>
    </build>

    <modules>
        <!-- 公共组件 -->
        <module>src/common</module>

        <!-- 微服务基础设施 -->
        <!-- 架构治理：退役 registry 与 config-center。
             原因：registry 无 @EnableEurekaServer 且 Nacos discovery 已禁用（空壳）；
                   config-center 是 Spring Cloud Config Server，但所有服务均使用 Nacos 配置中心。
             两者与 Nacos（注册+配置）完全冗余，保留源码供参考但不参与构建部署。 -->
        <!-- <module>src/registry</module> -->
        <!-- <module>src/config-center</module> -->
        <module>src/gateway</module>

        <!-- 核心业务服务 -->
        <module>src/core-services/erp</module>
        <module>src/core-services/crm</module>
        <module>src/core-services/mes</module>
        <module>src/core-services/wms</module>
        <module>src/core-services/srm</module>
        <module>src/core-services/hr</module>
        <module>src/core-services/oa</module>
        <module>src/core-services/aps</module>
        <module>src/core-services/ems</module>
        <module>src/core-services/scrm</module>
        <module>src/core-services/opportunity</module>
        <module>src/core-services/scm</module>
        <module>src/core-services/plm</module>
        <module>src/core-services/bom</module>
        <module>src/core-services/qms</module>
        <module>src/core-services/agv</module>
        <module>src/core-services/les</module>
        <module>src/core-services/scada</module>
        <module>src/core-services/eam</module>
        <!-- AI决策中枢（自动化刻度盘方案，P0骨架） -->
        <module>src/core-services/ai-brain</module>

        <!-- 业务支持服务 -->
    </modules>
</project>
