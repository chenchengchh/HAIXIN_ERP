spring:
  datasource:
    url: jdbc:mysql://mysql:3306/scrm_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: scrm_app
    password: root
  redis:
    host: redis
    port: 6379
    password: 
    database: 0
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: nacos:8848
      config:
        enabled: true
        server-addr: nacos:8848
        file-extension: yml
        group: DEFAULT_GROUP
        import-check:
          enabled: false
  config:
    import: optional:nacos:nacos:8848
